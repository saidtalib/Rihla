name: rihla
description: "Rihla – Trip organization app for adventurous groups."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # Firebase
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5
  firebase_auth: ^5.5.1
  firebase_storage: ^12.4.4

  # Monetisation
  purchases_flutter: ^8.9.3
  google_mobile_ads: ^5.3.0

  # AI
  google_generative_ai: ^0.4.6

  # Maps & Location
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.2

  # Media
  image_picker: ^1.1.2

  # Utilities
  uuid: ^4.5.1
  http: ^1.2.2
  intl: ^0.19.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/jeep.png
    - assets/icons/plane.png
    - assets/icons/train.png
    - assets/icons/ferry.png
    - assets/icons/tent.png
